# 카카오맵 테스트 체크리스트

## ✅ 설정 확인 완료

- [x] `.env` 파일에 `VITE_KAKAO_REST_API_KEY` 설정됨
- [x] `.env` 파일에 `VITE_KAKAO_JAVASCRIPT_KEY` 설정됨
- [x] `KakaoMap` 컴포넌트 생성됨
- [x] `CreateGroupModal`에서 카카오맵 사용하도록 수정됨

## 🧪 테스트 단계

### 1단계: 클라이언트 재시작 (필수!)

**중요**: `.env` 파일을 수정했으므로 반드시 클라이언트를 재시작해야 합니다!

```bash
# 현재 실행 중인 클라이언트 중지 (Ctrl+C)
# 그 다음 재시작
cd client
npm run dev
```

### 2단계: 브라우저에서 테스트

1. **브라우저에서 `http://localhost:5173` 접속**
2. **F12 → Console 탭 열기** (디버깅용)
3. **"새 모임 만들기" 버튼 클릭**
4. **"지도 보기" 버튼 클릭**

### 3단계: 확인 사항

#### ✅ 정상 작동 시:
- [ ] **카카오맵이 표시됨** (OpenStreetMap이 아님!)
- [ ] 콘솔에 에러가 없음
- [ ] 마커가 지도 중앙에 표시됨
- [ ] 주소 입력 필드에 "서울시 강남구" 입력 → 800ms 후 마커가 이동함
- [ ] 마커를 드래그 → 주소 입력 필드에 주소가 자동 입력됨

#### ❌ 문제 발생 시:

**지도가 표시되지 않는 경우:**
1. 콘솔 확인:
   - "카카오맵 JavaScript 키가 설정되지 않았습니다" → `.env` 파일 확인
   - "카카오맵 스크립트 로드 실패" → Network 탭에서 `dapi.kakao.com` 요청 확인
2. 확인 사항:
   - 클라이언트를 재시작했는지 확인
   - JavaScript 키가 올바른지 확인 (REST API 키 아님!)

**마커가 이동하지 않는 경우:**
1. 콘솔 확인:
   - "카카오맵 API 키 확인: 설정됨 ✅" 메시지 확인
   - 주소 입력 시 API 호출 로그 확인
2. 확인 사항:
   - `VITE_KAKAO_REST_API_KEY`가 설정되어 있는지 확인
   - 클라이언트를 재시작했는지 확인

## 📝 예상 결과

### 정상 작동 시:
- 지도: **카카오맵** (한국 지도 스타일)
- 마커: 파란색 마커 (드래그 가능)
- 주소 입력 → 마커 이동 ✅
- 마커 드래그 → 주소 자동 입력 ✅

### 문제 발생 시:
- 지도가 표시되지 않음 → JavaScript 키 확인
- 마커가 이동하지 않음 → REST API 키 확인
- 콘솔에 에러 메시지 → 에러 내용 확인



