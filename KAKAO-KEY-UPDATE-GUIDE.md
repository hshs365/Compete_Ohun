# 카카오 API 키 업데이트 가이드

## ✅ 완료된 작업

### 1. .env 파일 업데이트
- **REST API 키**: `ebc67881a9b01376d1b0365689a27776` ✅
- **JavaScript 키**: `966de50d9809428a90270aa613b8619a` ✅

## 🔄 다음 단계

### 1단계: 클라이언트 재시작 (필수!)

`.env` 파일을 수정했으므로 **반드시 클라이언트를 재시작**해야 합니다.

```bash
# 현재 실행 중인 클라이언트 중지 (Ctrl+C)
cd client
npm run dev
```

### 2단계: JavaScript SDK 도메인 확인

새로운 앱으로 전환했으므로, JavaScript SDK 도메인도 다시 등록되어 있는지 확인하세요.

1. **"앱 설정" > "앱" > "플랫폼 키"** 페이지로 이동
2. **"JavaScript 키"** 섹션에서
3. **"JavaScript SDK 도메인"**에 `http://localhost:5173`이 등록되어 있는지 확인
4. 없으면 다시 등록

### 3단계: 추가 기능 신청 확인

새로운 앱으로 전환했으므로, 카카오맵 추가 기능도 다시 신청해야 할 수 있습니다.

1. **"앱 설정" > "앱" > "추가 기능 신청"** 페이지로 이동
2. **카카오맵**이 승인되었는지 확인
3. 승인되지 않았다면 다시 신청

### 4단계: 카카오맵 활성화 확인

1. **"제품 설정" > "카카오맵"** 페이지로 이동
2. **"사용 설정" > "상태"** 토글이 ON인지 확인
3. OFF라면 ON으로 변경

## 🧪 테스트

### 1단계: 브라우저 완전히 닫기
- 모든 브라우저 창 닫기

### 2단계: 클라이언트 재시작
```bash
cd client
npm run dev
```

### 3단계: 브라우저 다시 열기
- `http://localhost:5173` 접속

### 4단계: 확인
1. **F12 → Console 탭**
2. **에러가 없는지 확인**
3. **F12 → Network 탭**
4. 필터에 "kakao" 입력
5. `dapi.kakao.com/v2/maps/sdk.js` 요청 확인
6. **상태 코드가 200 OK**인지 확인

## 📋 전체 체크리스트

### 필수 설정:
- [x] **.env 파일 업데이트**: 새로운 키 값으로 변경 완료 ✅
- [ ] **클라이언트 재시작**: 환경 변수 로드를 위해 필수
- [ ] **JavaScript SDK 도메인 등록**: `http://localhost:5173` 확인
- [ ] **추가 기능 신청**: 카카오맵 승인 확인
- [ ] **카카오맵 활성화**: 상태 ON 확인

## ⚠️ 주의사항

1. **클라이언트 재시작 필수**
   - `.env` 파일 변경 후 반드시 클라이언트를 재시작해야 합니다
   - 재시작하지 않으면 이전 키 값이 계속 사용됩니다

2. **브라우저 캐시**
   - 브라우저를 완전히 닫고 다시 열어야 합니다
   - 또는 Ctrl+Shift+R로 강력 새로고침

3. **변경 사항 반영 시간**
   - 카카오 개발자 콘솔에서 설정 변경 후 1-2분 소요될 수 있음

## 🆘 문제 해결

### 여전히 이전 키가 사용되는 경우:
1. **클라이언트 재시작 확인**
2. **브라우저 완전히 닫고 다시 열기**
3. **브라우저 캐시 클리어** (Ctrl+Shift+R)

### 스크립트 로드 실패가 발생하는 경우:
1. **JavaScript SDK 도메인 확인**
   - `http://localhost:5173`이 등록되어 있는지 확인
2. **추가 기능 신청 확인**
   - 카카오맵이 승인되었는지 확인
3. **카카오맵 활성화 확인**
   - 상태가 ON인지 확인



